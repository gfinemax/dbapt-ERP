# 구현 진행 기록

전체 요청: [requirements.md](requirements.md). 원본·상태·금액·메뉴 설계: [design.md](design.md).

## 범위와 현재 상태

전체 목표는 유지한다. 단계별 코드 커밋은 화면 또는 운영 적용 완료를 뜻하지 않는다.

| 단계 | 상태 | 확인된 결과 / 남은 일 |
| --- | --- | --- |
| 0 조사·설계 | 완료 | 현재 저장 구조·서버 경로·권한 위험 확인, 원본/상태/금액/URL 표 작성 |
| 1 공통 연결·지급 기반 | 완료 (운영 미적용) | 원본 연결·실제 입출금·여러 지출 배분·반납·배분 정정·조직/역할·동시 처리 검증 통과. 계약/화면 연결은 다음 단계 |
| 2 신탁 | 미완료 | 정규화 테이블만 준비됨. 계약 설정 명령·항목별 회신·재제출·철회·파일/출력 구현 필요 |
| 3 회계·정산·수납 | 미완료 | 기존 전표/개인 경비/예산 재사용 예정. 신규 원장과 기존 집계/수납 고유 ID 연결 필요 |
| 4 화면·메뉴 | 미완료 | 기존 미커밋 사이드바/점검 화면 초안 보존 중. 실제 기능 연결·공통 등록·URL/권한/모바일 검증 필요 |
| 5 통합·운영 적용 | 미완료 | 전체 시나리오, 운영 자료 대조, 마이그레이션 및 배포 확인 필요 |

## 1단계 구현

- 기존 결의/간편지출/개인 정산 원본은 유지하고 조직+원본 유형+원본 ID로 연결한다.
- 과거 지급완료인데 금액이 없으면 지급액을 추정하지 않는다. 기존 완료 표시와 확인 필요를 함께 유지한다.
- 실제 은행/현금 지급 기록과 지출 배분을 분리한다. 한 지급의 여러 지출 배분, 분할지급, 원지급에 대한 반환, 배분 정정 이력을 지원한다.
- 계약 미설정은 지급 배분 실행만 제한한다. 원본 조회·연결·실제 입출금 기록은 가능하다.
- 은행 원본 ID, 현금 지급 증빙 ID, 조직별 처리키로 중복 등록을 막는다. DB 잠금으로 동시 잔액 초과를 막는다.
- 이전 경로가 통합 연결된 지급액을 직접 덮어쓰거나 원은행 거래를 바꾸지 못하게 DB 트리거로 제한한다. 이 트리거는 원본을 실제로 연결한 뒤 적용된다.
- 같은 조합의 계좌이체를 비용·새 납부·예산 집행으로 생성하지 않는다.
- API에서 사용자/조직을 입력값으로 신뢰하지 않고 기존 Auth 검증과 구성원 조회를 재사용한다. DB 명령도 권한을 재검사한다. 신규 테이블은 서비스 경로 전용 정책과 권한을 적용한다.

## 검증

- `node node_modules/vitest/vitest.mjs run src/features/finance/fund-workflow-domain.test.ts src/features/finance/fund-workflow-repository.test.ts src/features/finance/reimbursement-auth.test.ts`: 3개 파일, 31개 테스트 통과.
- 변경 TypeScript 파일·테스트·격리 검증 스크립트 ESLint 통과.
- `node node_modules/next/dist/bin/next build`: 프로덕션 빌드 통과. 빌드 시점 작업 폴더에는 미커밋 화면 초안도 포함되어 있으므로 화면 완료 증거로 사용하지 않는다.
- 별도 `tsc --noEmit`은 기존 테스트 파일의 타입 오류 14건으로 실패. 변경 전 `3071f40`의 별도 checkout에서도 동일 14건 재현. 신규 기반 파일 오류는 없음. 이 검사는 통과로 보고하지 않는다.
- `node scripts/test-finance-workflow.mjs`: 최종 코드에서 통과. 운영 자격증명 없이 새 Docker PostgreSQL을 만들고 전체 로컬 스키마/마이그레이션을 적용한다. 개인 정산, 통합 예산, 부분 예약 기존 SQL 테스트와 새 지급 SQL 테스트, 별도 세션 동시 배분/동일 처리키 재시도, 실제 DB 역할 접근거절을 검증한다. 현금 중복, 다른 조직 계좌, 다른 계약의 승인 재사용, 미래 지급/이체, 사용 전 환급/지급 전 회수도 거절되는지 확인했다. 실행 후 해당 스크립트 소유 컨테이너만 정리한다.
- 화면 저장·새로고침, 메뉴/활성/모바일, 신탁 양식/첨부, 운영 배포는 아직 검증하지 않았다.

## 보존 기준과 운영 적용

2026-09-08 운영 읽기 조회에서 결의 6건/32,120,120원, 간편지출 5건/88,660원, 전표 0건, 결의 증빙 4건을 확인했다. 당시 전체 원본 행 지문:

| 원본 | MD5 (변경 탐지용) |
| --- | --- |
| 결의 | 8ba3eec2c82a54fdc833bbbb820f9c2a |
| 간편지출 | edef00acc61abb8c01383890411ad9ca |
| 전표 | d41d8cd98f00b204e9800998ecf8427e |
| 결의 증빙 메타데이터 | 180e4fd92c9f14b55a710686cae1ff7f |

파일 내용 보존·전체 연결의 최종 대조는 운영 적용 단계에서 별도로 수행한다. 운영 업무자료를 삭제/수정하거나 검증용 지급·결재·신탁 제출을 하지 않았다. 새 마이그레이션은 현재 격리 테스트 DB에만 적용했다.

시작 시 미커밋 변경은 `.tmp-repos/finance-goal-baseline-20260908`에 원본 바이트로 보존했다. 소스 백업은 테스트/컴파일에 혼입되지 않도록 `.snapshot` 확장자를 붙였다. 초기 JSON 한 행 방식의 미적용 SQL 초안은 정규화 마이그레이션으로 대체했다.

## 다음 단계

계약 버전과 적용 근거를 설정하는 서버 명령, 기존 지출을 선택하는 신탁 요청, 항목별 심사/보완/부분승인, 제출 스냅샷과 첨부 버전, 철회 확인과 재제출을 구현한다. 계약 자료가 없는 항목은 설정 필요로 유지하되 초안·조회는 제공한다. 운영비 선교부·기간잠금·자동 계정분개는 근거 없는 기본 정책으로 활성화하지 않는다.

## 2단계 진행: 신탁 저장·처리 기반

2단계는 저장·처리 기반(2a)과 실제 업무 화면 연결(2b)로 검증 단위를 나눈다. 화면까지 연결하기 전에는 2단계 전체 완료로 표시하지 않는다.

- 계약 초안/확인/폐기/새 버전, 관리계좌·운영계좌·적용 거래·필수서류·공동동의·한도·운영비 선교부 조건을 저장한다. 정책 미확정 초안은 저장 가능하며 제출/집행에만 확인 조건을 적용한다.
- 확인된 계약과 참조 계좌의 중요정보는 보존한다. 변경은 새 계좌/계약 버전으로 남긴다.
- 요청과 항목을 저장하고, 제출 시 계약/내부 승인/중복 요청액을 DB에서 확인한다. 원본과 계약, 선택 항목, 첨부 해시를 제출 버전으로 고정한다.
- 항목별 부분승인·보완·반려와 보완 항목만 재제출, 철회 요청/확인을 지원한다. 철회 확인 전에는 예약을 해제하지 않는다.
- 원본 감액 이후에는 자기 예약까지 포함한 전체 잔액으로 재제출 한도를 검사한다. 일부 지급 후 재검토는 기존 실제 지급을 보존하고 미지급 부분만 예약한다. 신탁사 회신은 당시 제출본의 사실로 보존하고 현재 원본이 달라지면 추가 지급을 제한한다.
- 요청 번호와 초안/제출본 인쇄 경로를 제공한다. 재제출 출력은 이번에 선택한 항목만 포함하며 원본 문서번호와 마스킹 계좌를 사용한다. 요청 전체 감사 스냅샷은 별도 보존한다.
- 첨부는 3MiB 이하 실제 PDF/PNG/JPEG/WebP 바이트를 검사하고 비공개 저장소에 덮어쓰기 없이 저장한다. 재시도는 기존 바이트의 SHA-256 일치까지 확인한다. 메타데이터 저장이 불명확한 경우 파일을 삭제하지 않는다. 다운로드는 조직/역할 확인 후 60초 서명 링크로 제공한다.
- 업로드/서명 링크는 공식 SDK 문서를 확인하여 구현했다: [업로드](https://supabase.com/docs/reference/javascript/file-buckets-upload), [다운로드 서명 링크](https://supabase.com/docs/reference/javascript/file-buckets-createsignedurl).

현재 검증: 관련 도메인/저장소/첨부/출력 단위 테스트 155개와 출력 HTTP 경로 테스트 24개 통과, 변경 파일 ESLint 통과. 프로덕션 빌드도 통과했다(미커밋 기존 화면 초안 포함). 별도 TypeScript 검사에서 신규 파일 오류는 수정했으며 기존 14건은 남아 있다. 격리 PostgreSQL에서 기존 SQL 회귀와 신탁 저장·재조회·권한·버전 잠금·혼합 회신·재제출·철회·감액·부분지급 재검토를 통과했다. 별도 동시 세션에서 1,000원 거래에 600원 요청 두 건 중 하나만 제출되고 같은 처리키 재시도에도 제출본 하나만 유지되는 것을 확인했다.

미검증/미완료: 실제 Storage 서비스와 브라우저 업로드, 요청·계약 설정 업무 화면, 운영 마이그레이션/배포. 운영 DB에는 새 기반을 적용하지 않았다.
