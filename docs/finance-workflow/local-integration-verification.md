# 격리 통합 검증 — 2026-09-08

운영 데이터 대신 별도 로컬 Supabase의 실제 Auth·REST·Storage와 Next 개발 서버를 사용했다. 운영 적용 완료를 뜻하지 않는다.

## 환경과 재현 자료

- Supabase 프로젝트: `dbapt-finance-e2e-20260908-a7c1`, API `http://127.0.0.1:56321`, DB 포트 `56322`.
- Next: `http://127.0.0.1:56330`. 자식 프로세스 환경변수로 모든 Supabase URL·키를 로컬 값으로 덮어썼다. 원본 `.env.local`을 수정하지 않았다.
- 계정: `example.invalid` 가짜 관리자·조회자. 관리자 API로 생성하고 이메일을 보내지 않았다.
- 원본 `schema.sql`과 지급 업무 읽기 migration까지 적용하고, 아래 계약 수정 migration을 추가 적용했다. 후속 검증에서 회계 migration `20260908030038_accounting_drafts.sql`도 로컬에 적용했다.
- 재현 보조 파일은 Git 제외 경로 `.tmp-repos/finance-e2e-local/`에 있다: `prepare-local.mjs`, `apply-local.mjs`, `seed-local.mjs`, `start-next.mjs`, `verify-storage.mjs`, `verify-trust-api.mjs`, `verify-permissions.mjs`, `verify-payment-api.mjs`. 일부는 생성한 fixture를 전제로 하는 순차 검증 파일이다. 운영 환경에서 실행하지 않는다.
- 인증키·비밀번호·세션은 위 제외 경로의 private 파일에만 저장했다. 로그·문서에 인증값을 포함하지 않는다.

## 확인한 결과

| 대상 | 실제 검증 | 결과 |
| --- | --- | --- |
| 로그인 | 브라우저 로그인 후 실제 Auth 사용자와 조합 역할로 신탁·설정 화면 조회 | 통과 |
| 계약 초안 | 브라우저로 미확정 계약 생성, 원본 PDF 첨부, 이후 페이지 재조회 | 통과 |
| Storage | 브라우저 업로드→메타데이터 1건→원본과 다운로드 바이트·SHA-256 일치 | 통과 |
| 파일 열기 | UI 다운로드 준비 후 1분 유효 링크 표시, 실제 서명 URL 200과 원본 바이트 일치 | 통과 |
| 접근 통제 | 익명·인증된 조회자 Storage 직접 다운로드 거부, 직접 테이블/RPC 접근 거부, 서버 RPC의 조회자 계약 수정 거부 | 통과 |
| 기존 지출 연결 | 브라우저에서 승인된 가짜 지출결의 원본을 연결 | 통과 |
| 신탁 초안 | 브라우저에서 600원 항목 초안 저장 후 새로고침, 목록 1건·원래 금액 유지 | 통과 |
| 계약 확정·제출 | 실제 REST로 계약 조건 수정/확정, 경로 적용, 1,000원 요청 제출 | 통과 |
| 중복 요청 | 동일 제출 처리키를 두 번 호출한 뒤 제출 원본 1건 확인 | 통과 |
| 부분 승인·지급 | 실제 REST와 DB에서 1,000원 지출 중 신탁 승인 600원, 지급 300원, 같은 배분키 재시도 후 배분 1건 확인 | 통과 |
| 지급 재조회 | 브라우저에서 누적 지급 300원·미지급 700원·추가 지급 가능 300원·부분지급 1건 표시 확인 | 통과 |
| 지급 필터 | 부분지급 버튼 클릭 후 `?tab=PARTIAL` 이동 확인 | 통과 |
| 회계 초안 | 브라우저에서 원본 선택·귀속일·메모 입력 후 생성, 초안 수정에서 메모·차변 1,000원 분개 추가, 저장·새로고침 | 통과 |
| 회계 재조회 | 동일 전표 1건, lock version 2, 수정 메모·분개 1행·승인대기 상태를 실제 DB와 화면에서 확인 | 통과 |
| 통합 지출 원본 | 새 미연결 750원 지출결의 선택→연결→새로고침. 원본 전체 JSON 불변과 연결 레코드 1건 확인 | 통과 |
| 지출 상세 연결 | 기존 지급300원 원본의 신탁2건·전표1건 링크 표시, 회계 링크 클릭으로 동일 voucherId 이동 | 통과 |
| 업무현황 | 지급가능 1건 카드 클릭·목록1건·task URL 일치, 회계 카드 Enter, 검색 후 Space로 전체보기·검색 초기화, 새로고침 후 전체5건 확인. 회계 카드 Space 선택·새로고침 후 선택 상태와 목록4건 유지 | 통과 |
| 사이드바 | 실제 DOM의 15개 메뉴를 요구한 순서와 정확히 대조 | 통과 |
| 증빙·점검 HTTP | 인증된 실제 GET으로 신탁/개인 증빙·세금계산서·회계일 미등록 월점검·환급·수납 6개 조건 200 및 예상 안내 확인 | 통과 |
| 통합 증빙 다운로드 | 실제 앱 다운로드 route 303·private/no-store, 로컬 서명 URL 바이트 일치, 비로그인 401 확인 | 통과 |
| 증빙 원본 선택 | 브라우저 원본 종류를 신탁·지급으로 선택하고 조회, 실제 등록된 4건 및 원본 연결·다운로드 링크 확인 | 통과 |
| 월점검 범위 | 브라우저에서 회계일 미등록 전체기간 선택·조회, 원본 3건과 실제 금액 표시. 기간잠금과 개인 정산월마감 구분 안내 확인 | 통과 |
| 수납·환급 미연결 | 브라우저에서 원장 연결 필요·수납액 미집계 안내 확인. 환급은 검토 대상 거래 0건으로만 표시하고 확정 환급 실적으로 표현하지 않음 | 통과 |

부분 승인과 실제 지급 배분은 이 검증에서 REST를 통해 입력했다. 해당 단계의 브라우저 입력 전체를 통과했다고 표현하지 않는다. 후속 검증에서는 별도 미지급 500원 가짜 원본으로 최초 제출 → 보완 회신 → 재제출 → 철회 요청 → 신탁사 철회 확인을 실제 브라우저 폼에서 입력·저장하고 새로고침했다. 제출 1·2차 보존, 철회 요청 중 예약 500원 유지·요청 가능 0원, 최종 철회 후 예약 0원·요청 가능 500원 복구를 DB 재조회로 확인했다.

- 신탁 브라우저 최종 철회 후 재조회 캡처: `C:/Users/finemax/.agent-browser/tmp/screenshots/screenshot-1788837477186.png`

## 발견·수정한 결함

기존 계약 초안을 수정할 때 `trust_command`의 지역 변수 `conditions`와 테이블의 같은 이름 컬럼이 충돌하여 PostgreSQL이 `column reference "conditions" is ambiguous` 오류를 반환했다. 최초 생성만 확인하면 발견되지 않는 수정 경로 결함이었다.

`20260908030647_trust_contract_condition_update.sql`에서 지역 변수를 `v_conditions`로 분리했다. 기존 컬럼명·JSON 키·저장 계약·권한·동시성 규칙을 유지한다. 실제 로컬 Supabase에 적용한 후 REST 수정·확정·제출에 성공했다.

`supabase/tests/trust_request_versions.sql`에는 기존 초안 조건 변경 저장·재조회, 동일 키 재시도, 오래된 버전 수정 거부, 감사 이벤트 중복 방지 회귀를 추가했다. 실제 Supabase PostgreSQL에서 기존 시나리오와 새 회귀 DO 블록 모두 통과했다.

## 화면 캡처

로컬 머신의 검증 이미지이며 저장소에 인증 자료를 포함하지 않는다.

- 초안 입력: `C:/Users/finemax/.agent-browser/tmp/screenshots/screenshot-1788836429519.png`
- 신탁 초안 저장 후 재조회: `C:/Users/finemax/.agent-browser/tmp/screenshots/screenshot-1788836762842.png`
- 회계 초안 수정 후 재조회: `C:/Users/finemax/.agent-browser/tmp/screenshots/screenshot-1788837670136.png`
- 업무현황 전체보기·검색 초기화 후: `C:/Users/finemax/.agent-browser/tmp/screenshots/screenshot-1788838018505.png`
- 신탁·지급 증빙 목록: `C:/Users/finemax/.agent-browser/tmp/screenshots/screenshot-1788838243602.png`
- 회계일 미등록 월점검: `C:/Users/finemax/.agent-browser/tmp/screenshots/screenshot-1788838287928.png`
- 부분 지급 재조회: `C:/Users/finemax/.agent-browser/tmp/screenshots/screenshot-1788836982249.png`

## 운영 적용과 남은 확인

운영 Supabase에 migration을 적용하거나 운영 승인·송금·신탁 제출·메일 발송을 하지 않았다. 운영 데이터·첨부 보존 검증과 실제 배포 확인은 별도 단계다. 회계 확정·역분개, 환급 실행·선지급 정산 승인/완료, 메뉴별 모든 업무 실행은 이 검증의 완료 범위에 포함되지 않는다. 회계 초안은 미확정 계정·차대변 차이를 보관하는 의도된 상태로 검증했으며 확정 실적에 반영하지 않았다.

## 후속 업무현황·지급 URL·선지급 실화면 검증

- 완료된 `20260908032705_finance_task_sources.sql`과 `20260908032253_advance_settlement_drafts.sql`을 격리 DB에 적용했다. 업무현황 함수의 예외 범위를 좁힌 최종본도 로컬에서 재적용하고 페이지 오류 없음과 신탁 요청 준비 카드 표시를 확인했다.
- 추가 업무 카드가 표시되고, 신탁 요청 준비 2건을 클릭하면 `task=TRUST_READY`와 실제 목록 2건이 일치했다. 내 결재 등 0건 카드도 조회 실패 없이 표시했다. 이 수치는 가짜 fixture 기준이다.
- 기존 지급대기/완료 URL의 실제 HTTP 응답은 각각 307이며 `/finance/payments`로 이동한다. `q=LOCAL&from=old`를 보존하고 `tab=UNPAID` 또는 `tab=PAID`를 설정했다.
- 미연결 지급완료 원본 900원은 브라우저에서 ‘통합 연결 전 기존 기록’, ‘지급완료 · 원본 금액 900원 · 실제 지급액은 원본 근거 확인’으로 표시됐다. 실제 지급 내역으로 합산하지 않았다.
- 선지급 화면에서 실제 담당자 선지급 원본 선택, 지급 배분의 ‘최초 선지급’ 명시 선택, 기존 사용내역 600원 선택, 검토 메모 입력으로 초안을 저장했다. 다시 수정하여 메모를 변경하고 새로고침했다.
- DB 재조회 결과 동일 초안 1건·lock version 2, 최초 지급 1,000원·추가 지급 0원·실제 반납 200원·초안 사용 600원·차액 200원이 일치했다. 결의서 원본 전체 JSON과 실제 지급 2행은 저장 전후 완전히 동일했다. 정산 승인·완료는 정책 확인 필요로 비활성 상태였다.
- 재현 보조 파일: `.tmp-repos/finance-e2e-local/check-legacy-redirect.mjs`, `seed-advance-ui.mjs`, `check-advance-ui.mjs`. 생성 스크립트는 일회성 로컬 fixture용이며 운영에 실행하지 않는다.
- 선지급 수정 후 새로고침 캡처: `C:/Users/finemax/.agent-browser/tmp/screenshots/screenshot-1788839034890.png`.

기존 대형 지출결의서 작성 화면의 새 기본값은 이번 실제 브라우저 검증에서 확인하지 못했다. 격리 DB의 SQL 검증용 최소 `resolution_data={}` fixture가 해당 기존 화면의 전체 문서 shape를 충족하지 않아 목록 초기화에서 실패했다. 이를 제품의 운영 데이터 오류로 판정하지 않았고, 이미 원본 불변 검증에 사용한 fixture를 대량 변경하지 않았다. 해당 기본값의 자동화 테스트 결과와 실제 화면 검증 범위를 구분해야 한다.
