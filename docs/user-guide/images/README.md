# 이미지 추가 지침

- 공통 메뉴 이미지는 `common/`에 한 번만 저장한다.
- 기능별 이미지는 `approval/`, `finance/`, `members/`, `admin/`처럼 나눈다.
- 개인정보, 계좌번호와 실사용 문서번호는 반드시 가린다.
- 이미지에는 클릭 순서 번호를 표시하고 문서에는 같은 번호의 설명을 작성한다.
- UI가 안정된 정식 배포 시점에 전체 이미지를 다시 촬영한다.
